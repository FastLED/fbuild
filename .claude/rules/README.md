# Claude Code Rules

Path-scoped and global rules loaded by Claude Code on demand. Rules without `paths:` frontmatter are always loaded. Rules with `paths:` are loaded only when matching files are accessed.

## Contents

- **`workflow.md`** -- Plan mode, subagent strategy, self-improvement, verification, autonomous bug fixing, task management, and A/B testing rules
