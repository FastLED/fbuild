# Code Review Skill

End-of-session code review that checks changed code for:
- Hardcoded values that should be in JSON config files
- Logic that belongs in `fbuild-core` instead of platform-specific crates
- Bugs and correctness issues
