[build-system]
requires = ["setuptools>=65.5.1", "setuptools-scm", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fbuild"
readme = "README.md"
description = "Modern replacement for PlatformIO with URL-based platform/toolchain management and bug-free architecture"
requires-python = ">=3.10"
keywords = ["embedded", "arduino", "platformio", "compiler", "toolchain", "firmware", "microcontroller"]
license = { text = "BSD 3-Clause License" }
classifiers = ["Programming Language :: Python :: 3"]
dependencies = [
    "requests>=2.28.0",
    "tqdm>=4.64.0",
    "pyserial>=3.5",
    "esptool>=4.6.0",
    "psutil>=5.9.0",
    "fasteners>=0.18",
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "websockets>=12.0",
    "httpx>=0.26.0",
    "pydantic>=2.0.0",
]
# Version is centralized in src/fbuild/__init__.py - keep in sync
version = "1.3.37"

[project.optional-dependencies]
dev = [
    "pytest>=7.0,<9.0",
    "pytest-cov",
    "pytest-xdist",
]

[dependency-groups]
test = [
    "pytest>=7.0,<9.0",
    "pytest-cov",
    "pytest-xdist",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["fbuild*"]
exclude = ["fbuild_lint*"]

[tool.setuptools.package-data]
fbuild = ["py.typed", "platform_configs/**/*.json", "platform_configs/manifest.json"]

[tool.ruff]
line-length = 200
extend-exclude = ["**/.zap/**", "**/.fbuild/**", "**/.build/**"]

[tool.black]
line-length = 200
extend-exclude = '''
/(
    \.zap
  | \.fbuild
  | \.build
)/
'''

[tool.pylint."MESSAGES CONTROL"]
good-names = [
    "c",
    "i",
    "ok",
    "id",
    "e",
    "f"
]
disable = [
    "missing-function-docstring",
    "missing-module-docstring"
]

[tool.isort]
profile = "black"
skip_glob = [
    "*/.fbuild/**",
    "*/.build/**",
    "*/.zap/**",
    "*/build/**",
    "*/__pycache__/**",
    "*/.venv/**",
    "*/.git/**",
    "*/.pytest_cache/**",
    "*/.mypy_cache/**",
    "*/.ruff_cache/**",
    "*.egg-info/**",
]

[tool.mypy]
ignore_missing_imports = true
disable_error_code = ["import-untyped"]

[tool.pyright]
# Basic type checking (current default)
typeCheckingMode = "basic"
reportMissingImports = "none"
extraPaths = ["src"]
# Exclude test directories from strict parameter type checking (pytest fixtures don't need types)
exclude = ["**/test_*.py", "**/tests/**", "**/.zap/**", "**/.fbuild/**", "**/.build/**"]

# Enable useful warnings that catch real bugs
reportUnusedImport = "warning"
reportUnusedVariable = "warning"
reportUnusedClass = "warning"
reportUnusedFunction = "error"
reportUnnecessaryTypeIgnoreComment = "warning"
reportUnnecessaryIsInstance = "warning"
reportUnnecessaryComparison = "error"
reportUnnecessaryCast = "warning"
reportImplicitStringConcatenation = "warning"

# Enforce parameter and return type annotations (but only for missing annotations, not unknown types)
reportMissingParameterType = "error"

[project.scripts]
fbuild = "fbuild.cli:main"

[tool.pytest.ini_options]
markers = [
    "integration: marks tests as integration tests (deselected by default, run with --full)",
    "concurrent_safety: marks tests as concurrent safety tests (deselected by default, run with --full)",
    "hardware: marks tests that require ESP32 hardware",
    "unit: marks tests as unit tests (fast, no external dependencies)",
    "esp32s3: marks tests specific to ESP32-S3 hardware",
    "diagnostic: marks tests as diagnostic/utility tests (always run, provide debugging info)",
]
# By default, skip integration and concurrent_safety tests
addopts = "-m 'not integration and not concurrent_safety'"
testpaths = ["tests"]
