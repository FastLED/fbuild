# ESP32-S3 Test Fixture

Xtensa LX7 build validation project for the ESP32-S3-DevKitC-1 board. Uses the Arduino framework on the `espressif32` platform with native USB-CDC enabled (`ARDUINO_USB_CDC_ON_BOOT=1`).
