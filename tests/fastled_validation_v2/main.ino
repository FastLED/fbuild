// FastLED Validation sketch - bridges fbuild to FastLED validation tests
// This file includes the FastLED Validation.ino sketch directly so fbuild can compile it

// The Validation sketch files have been copied to src/
// This allows fbuild to compile them without complex path resolution

// Include the validation sketch and all its dependencies
#include "src/Validation.ino"
