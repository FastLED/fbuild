; Test project for LTO dead code elimination with FastLED
; Tests whether unused code from FastLED (like stb_vorbis tables) is eliminated

[env:uno]
platform = atmelavr
board = uno
framework = arduino
lib_deps =
    https://github.com/FastLED/FastLED#master
