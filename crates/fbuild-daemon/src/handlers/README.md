# Handlers

HTTP and WebSocket route handlers for the fbuild daemon.

- `health.rs` — Health check, daemon info, shutdown
- `operations.rs` — Build, deploy, monitor endpoints
- `devices.rs` — Serial device discovery
- `websockets.rs` — WebSocket serial monitor
