# STM32

STM32 platform build support (STM32F1, STM32F4, STM32H7 families).

Uses the generic ARM compiler/linker from `generic_arm/` with STM32-specific
MCU configurations and the STM32 Arduino cores framework.
