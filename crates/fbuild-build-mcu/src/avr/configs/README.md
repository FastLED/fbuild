# AVR MCU Configs

Data-driven configuration for AVR ATmega MCUs, embedded at compile time via `include_str!()`.

## Files

- **`avr.json`** -- Compiler and linker flags for AVR ATmega MCUs
- **`reference/`** -- PlatformIO-extracted reference configs for validation
