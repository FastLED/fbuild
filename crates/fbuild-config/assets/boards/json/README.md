# Board JSON Files

Individual board definition files from the PlatformIO registry. Each file is named `{board_id}.json` and contains: id, name, platform, mcu, fcpu, ram, rom, frameworks, vendor, url, and optional debug/connectivity fields.
