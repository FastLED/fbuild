# SAM MCU Configs

Data-driven configuration for Atmel SAM boards (ARM Cortex-M3), embedded at compile time via `include_str!()`.

## Files

- **`sam3x.json`** -- Flags for SAM3X8E (Cortex-M3, Arduino Due)
