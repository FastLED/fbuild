# NRF52 MCU Configs

Data-driven configuration for Nordic NRF52 boards (ARM Cortex-M4F), embedded at compile time via `include_str!()`.

## Files

- **`nrf52840.json`** -- Flags for NRF52840 (Cortex-M4F)
