# PlatformIO Reference Configs — Future Platforms

Authoritative build flags extracted from PlatformIO for platforms that don't
have fbuild-build modules yet. These serve as the spec for future implementation.

Platforms with existing modules store their references in
`src/<platform>/configs/reference/` instead.

See `ci/extract_pio_build_flags.py` for regeneration instructions.
