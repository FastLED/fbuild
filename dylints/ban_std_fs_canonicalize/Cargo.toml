[package]
name = "ban_std_fs_canonicalize"
version = "0.1.0"
description = "Ban std::fs::canonicalize — use fbuild_core::path::canonicalize_existing"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
dylint_linting = "6.0.1"

[dev-dependencies]
dylint_testing = "6.0.1"

[package.metadata.rust-analyzer]
rustc_private = true
