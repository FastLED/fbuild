[package]
name = "ban_std_fs_in_async"
version = "0.1.0"
description = "Ban std::fs::* in scopes where async is the convention (fbuild-daemon production code)"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
dylint_linting = "6.0.1"

[dev-dependencies]
dylint_testing = "6.0.1"

[package.metadata.rust-analyzer]
rustc_private = true
