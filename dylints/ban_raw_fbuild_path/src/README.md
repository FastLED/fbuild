# `ban_raw_fbuild_path` — source

The lint implementation is in [`lib.rs`](lib.rs). The baseline list of
legacy call sites is in [`allowlist.txt`](allowlist.txt).

See the crate-level [`../README.md`](../README.md) for the rationale and
the ratchet plan (FastLED/fbuild#1349).
