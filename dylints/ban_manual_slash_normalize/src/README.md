# `ban_manual_slash_normalize` — source

The lint implementation is in [`lib.rs`](lib.rs). The allowlist of
legacy call sites is in [`allowlist.txt`](allowlist.txt).

See the crate-level [`../README.md`](../README.md) for the rationale
and the migration plan.
