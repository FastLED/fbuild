name: Build Leonardo

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/leonardo/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-leonardo.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/leonardo/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-leonardo.yml'

concurrency:
  group: build-leonardo.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "Leonardo"
      test-dir: "tests/platform/leonardo"
      env-name: "leonardo"
      firmware-ext: "hex"
