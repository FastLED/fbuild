name: Build Nucleo F439ZI

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/nucleo_f439zi/**'
      - 'crates/fbuild-build-arm/src/stm32/**'
      - '.github/workflows/build-nucleo-f439zi.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/nucleo_f439zi/**'
      - 'crates/fbuild-build-arm/src/stm32/**'
      - '.github/workflows/build-nucleo-f439zi.yml'

concurrency:
  group: build-nucleo-f439zi.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "Nucleo F439ZI"
      test-dir: "tests/platform/nucleo_f439zi"
      env-name: "nucleo_f439zi"
      firmware-ext: "hex"
