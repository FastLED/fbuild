name: Build Raspberry Pi Pico

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/rpipico/**'
      - 'crates/fbuild-build-arm/src/rp2040/**'
      - '.github/workflows/build-rpipico.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/rpipico/**'
      - 'crates/fbuild-build-arm/src/rp2040/**'
      - '.github/workflows/build-rpipico.yml'

concurrency:
  group: build-rpipico.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "Raspberry Pi Pico"
      test-dir: "tests/platform/rpipico"
      env-name: "rpipico"
      firmware-ext: "bin"
