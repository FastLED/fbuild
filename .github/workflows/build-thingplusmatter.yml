name: Build SparkFun Thing Plus Matter

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/sparkfun_thingplusmatter/**'
      - 'crates/fbuild-build-arm/src/sam/**'
      - '.github/workflows/build-thingplusmatter.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/sparkfun_thingplusmatter/**'
      - 'crates/fbuild-build-arm/src/sam/**'
      - '.github/workflows/build-thingplusmatter.yml'

concurrency:
  group: build-thingplusmatter.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "SparkFun Thing Plus Matter"
      test-dir: "tests/platform/sparkfun_thingplusmatter"
      env-name: "thingplusmatter"
      firmware-ext: "bin"
