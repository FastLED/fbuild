name: Build LPCXpresso845-MAX

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/lpcxpresso845max/**'
      - 'crates/fbuild-build-arm/src/nxplpc/**'
      - 'crates/fbuild-build-arm/src/generic_arm/**'
      - '.github/workflows/build-lpcxpresso845max.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/lpcxpresso845max/**'
      - 'crates/fbuild-build-arm/src/nxplpc/**'
      - 'crates/fbuild-build-arm/src/generic_arm/**'
      - '.github/workflows/build-lpcxpresso845max.yml'

concurrency:
  group: build-lpcxpresso845max.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "NXP LPCXpresso845-MAX"
      test-dir: "tests/platform/lpcxpresso845max"
      env-name: "lpcxpresso845max"
      firmware-ext: "bin"
