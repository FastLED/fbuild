name: Build Teensy LC

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/teensylc/**'
      - 'crates/fbuild-build-arm/src/teensy/**'
      - '.github/workflows/build-teensylc.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/teensylc/**'
      - 'crates/fbuild-build-arm/src/teensy/**'
      - '.github/workflows/build-teensylc.yml'

concurrency:
  group: build-teensylc.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "Teensy LC"
      test-dir: "tests/platform/teensylc"
      env-name: "teensylc"
      firmware-ext: "hex"
