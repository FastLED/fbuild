name: Build ATtiny88

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/attiny88/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-attiny88.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/attiny88/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-attiny88.yml'

concurrency:
  group: build-attiny88.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "ATtiny88"
      test-dir: "tests/platform/attiny88"
      env-name: "attiny88"
      firmware-ext: "hex"
