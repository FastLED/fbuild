name: Build ESP32-H2

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/esp32h2/**'
      - 'crates/fbuild-build-esp/src/esp32/**'
      - '.github/workflows/build-esp32h2.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/esp32h2/**'
      - 'crates/fbuild-build-esp/src/esp32/**'
      - '.github/workflows/build-esp32h2.yml'

concurrency:
  group: build-esp32h2.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "ESP32-H2"
      test-dir: "tests/platform/esp32h2"
      env-name: "esp32h2"
      firmware-ext: "bin"
