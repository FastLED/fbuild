name: Build Nano Every

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/nano_every/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-nano-every.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/nano_every/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-nano-every.yml'

concurrency:
  group: build-nano-every.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "Nano Every"
      test-dir: "tests/platform/nano_every"
      env-name: "nano_every"
      firmware-ext: "hex"
