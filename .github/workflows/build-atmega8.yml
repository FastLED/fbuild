name: Build ATmega8

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/atmega8/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-atmega8.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/atmega8/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-atmega8.yml'

concurrency:
  group: build-atmega8.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "ATmega8"
      test-dir: "tests/platform/atmega8"
      env-name: "atmega8"
      firmware-ext: "hex"
