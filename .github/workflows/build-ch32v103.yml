name: Build CH32V103

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "CH32V103"
      test-dir: "tests/platform/ch32v103"
      env-name: "ch32v103"
      firmware-ext: "bin"
