name: Build LPCXpresso804

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/lpcxpresso804/**'
      - 'crates/fbuild-build-arm/src/nxplpc/**'
      - 'crates/fbuild-build-arm/src/generic_arm/**'
      - '.github/workflows/build-lpcxpresso804.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/lpcxpresso804/**'
      - 'crates/fbuild-build-arm/src/nxplpc/**'
      - 'crates/fbuild-build-arm/src/generic_arm/**'
      - '.github/workflows/build-lpcxpresso804.yml'

concurrency:
  group: build-lpcxpresso804.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "NXP LPCXpresso804"
      test-dir: "tests/platform/lpcxpresso804"
      env-name: "lpcxpresso804"
      firmware-ext: "bin"
