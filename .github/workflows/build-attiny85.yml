name: Build ATtiny85

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/attiny85/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-attiny85.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/attiny85/**'
      - 'crates/fbuild-build-mcu/src/avr/**'
      - '.github/workflows/build-attiny85.yml'

concurrency:
  group: build-attiny85.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "ATtiny85"
      test-dir: "tests/platform/attiny85"
      env-name: "attiny85"
      firmware-ext: "hex"
