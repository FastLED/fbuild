name: Build STM32F103TB

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/stm32f103tb/**'
      - 'crates/fbuild-build-arm/src/stm32/**'
      - '.github/workflows/build-stm32f103tb.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/stm32f103tb/**'
      - 'crates/fbuild-build-arm/src/stm32/**'
      - '.github/workflows/build-stm32f103tb.yml'

concurrency:
  group: build-stm32f103tb.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "STM32F103TB"
      test-dir: "tests/platform/stm32f103tb"
      env-name: "stm32f103tb"
      firmware-ext: "hex"
