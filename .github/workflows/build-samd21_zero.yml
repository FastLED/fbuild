name: Build Arduino Zero

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/samd21_zero/**'
      - 'crates/fbuild-build-arm/src/sam/**'
      - '.github/workflows/build-samd21_zero.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/samd21_zero/**'
      - 'crates/fbuild-build-arm/src/sam/**'
      - '.github/workflows/build-samd21_zero.yml'

concurrency:
  group: build-samd21_zero.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "Arduino Zero"
      test-dir: "tests/platform/samd21_zero"
      env-name: "samd21_zero"
      firmware-ext: "bin"
