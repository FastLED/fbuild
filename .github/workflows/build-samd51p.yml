name: Build SAMD51P

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/samd51p/**'
      - 'crates/fbuild-build-arm/src/sam/**'
      - '.github/workflows/build-samd51p.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/samd51p/**'
      - 'crates/fbuild-build-arm/src/sam/**'
      - '.github/workflows/build-samd51p.yml'

concurrency:
  group: build-samd51p.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "SAMD51P"
      test-dir: "tests/platform/samd51p"
      env-name: "samd51p"
      firmware-ext: "bin"
