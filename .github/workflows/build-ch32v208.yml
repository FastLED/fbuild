name: Build CH32V208

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/ch32v208/**'
      - 'crates/fbuild-build-mcu/src/ch32v/**'
      - '.github/workflows/build-ch32v208.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/ch32v208/**'
      - 'crates/fbuild-build-mcu/src/ch32v/**'
      - '.github/workflows/build-ch32v208.yml'

concurrency:
  group: build-ch32v208.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "CH32V208"
      test-dir: "tests/platform/ch32v208"
      env-name: "ch32v208"
      firmware-ext: "bin"
