name: Build SAMD21

# >>> RENDERED-ON-BEGIN (ci/render_workflows.py) -- do not edit by hand <<<
on:
  workflow_dispatch: {}
  workflow_call: {}
  push:
    branches: [main]
    paths:
      - 'tests/platform/samd21/**'
      - 'crates/fbuild-build-arm/src/sam/**'
      - '.github/workflows/build-samd21.yml'
  pull_request:
    branches: [main]
    paths:
      - 'tests/platform/samd21/**'
      - 'crates/fbuild-build-arm/src/sam/**'
      - '.github/workflows/build-samd21.yml'

concurrency:
  group: build-samd21.yml-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
# >>> RENDERED-ON-END <<<

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "SAMD21"
      test-dir: "tests/platform/samd21"
      env-name: "samd21"
      firmware-ext: "bin"
